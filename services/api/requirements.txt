# Human-edited inputs — lower-bound (`>=`) pins only; a regression test enforces
# that (tests/test_dependency_lock.py). The exact resolution lives in
# requirements.lock (used by setup and CI); regenerate it per
# docs/dev-workflows.md#python-dependency-updates after editing this file.
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
boto3>=1.35.0
Pillow>=11.0.0
PyPDF2>=3.0.0

# --- MONAI segmentation pipeline (local ML; imported lazily at runtime) ---
# Upper bounds are load-bearing: unbounded ML pins boot green but break the
# marquee segmentation on a clean install. Keep in sync with requirements.lock.
monai>=1.6.0,<1.7.0
torch>=2.4.0,<2.9.0
nibabel>=5.2.0,<6.0.0
pydicom>=2.4.0,<4.0.0
scikit-image>=0.24.0,<0.26.0
scipy>=1.13.0,<2.0.0
# Required by the Swin UNETR bundle (swin_unetr_btcv_segmentation): MONAI's
# SwinUNETR calls einops.rearrange via optional_import, so without einops the
# Swin model path fails at inference. The default spleen UNet does not need it.
einops>=0.7.0,<0.9.0
huggingface-hub>=0.24.0,<1.0.0
requests>=2.31.0,<3.0.0
numpy>=1.26.0,<2.0.0

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
